# We can use enumerate 

for count, value in enumarate(list):
    pass
# We can try to achieve our target, with the subtraction with it.

remainder = target - list[count]

# Palindrome is simple:
# Compare to lists contains strings.

def isPalindrome(self, x: int) -> bool:
    return str(x)==(str(x))[::-1]
  
# To represent something, you can use a dictionary.

# For ROMAN Numbers:
# val_map = {"0": 0, "I": 1, "V": 5, "X": 10, "L": 50, "C": 100, "D": 500, "M": 1000}

# You can pop elements from a list:
_list.pop(0)


# All returns true if condition is met:

if all(a[i]==c for a in strs):
    answer+=c
    
#  Set is one of 4 built-in data types in Python used to store collections of data, the other 3 are List, Tuple, and Dictionary, all with different qualities and usage.
# A set is a collection which is unordered, unchangeable*, and unindexed.
# Set items are unordered, unchangeable, and do not allow duplicate values.

seen = set()
